1. Які мови програмування підтримує CLR?
CLR підтримує багато мов програмування, такі як C#, F#, Visual Basic .NET, C++/CLI та ін.

2. З яких компонентів складається CLR?
CLR складається з декількох компонентів, включаючи Just-In-Time Compiler (JIT), Common Type System (CTS), Common Language Specification (CLS), Garbage Collector (GC) та ін.

3. Скільки рівнів має Garbage Collector?
Garbage Collector має кілька рівнів: 0-го, 1-го та 2-го рівня. Об'єкти в пам'яті управляються GC, який періодично проходить цими рівнями, звільняючи пам'ять.

4. Коли викликається останній рівень GC?
Останній рівень GC називається Finalizer і він викликається перед тим, як об'єкт видаляється з пам'яті. Це дозволяє виконувати додаткове очищення, наприклад, закрити файли або звільнити інші ресурси.

5. Що таке CLS?
CLS (Common Language Specification) – це набір правил, яким повинні дотримуватися мови програмування, щоб гарантувати, що їх код працюватиме в CLR. CLS визначає мінімальний набір функцій і типів, які повинні підтримуватися мовою, щоб вона могла взаємодіяти з іншими мовами в .NET.

1. У чому відмінність асинхронного і багатопоточного програмування?
Відмінність між асинхронним та багатопоточним програмуванням полягає у тому, що в асинхронному програмуванні використовуються не блокуючі операції, тобто виклики функцій, що не зупиняють виконання програми, тоді як у багатопоточному програмуванні використовуються потоки, які виконуються паралельно з основним потоком програми та можуть блокувати виконання програми.

2. Які типи даних може повертати async–await?
async-await може повертати об'єкти, які мають типи даних: None, bool, int, float, str, bytes, кортежі, списки, словники та інші.

3. Які модифікатори для параметрів не можна використовувати в асинхронних методах?
В асинхронних методах не можна використовувати модифікатори такі як "ref", "out", "in", оскільки ці модифікатори заборонені в асинхронних методах.

4. Які властивості надає клас Thread? Опишіть їх.
Клас Thread надає наступні властивості:
- name: назва потоку;
- daemon: потік може бути завершений, якщо всі інші потоки в програмі завершать свою роботу;
- ident: ідентифікатор потоку.

5. Які методи надає клас Thread? Опишіть їх.
Клас Thread надає наступні методи:
- start(): запускає потік;
- run(): код, що виконується потоком;
- join([time]): чекає, доки потік не завершить свою роботу;
- isAlive(): перевіряє, чи потік ще працює;
- getName(): повертає ім'я потоку;
- setName(name): задає ім'я потоку.
